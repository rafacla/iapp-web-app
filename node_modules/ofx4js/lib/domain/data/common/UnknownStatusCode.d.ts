import { Severity, StatusCode } from "./StatusCode";
/**
 * Holder for an unknown status code.
 */
export declare class UnknownStatusCode extends StatusCode {
    private code;
    private message;
    private defaultSeverity;
    constructor(code: number, message: string, defaultSeverity: Severity);
    getCode(): number;
    getMessage(): string;
    getDefaultSeverity(): Severity;
    toString(): string;
}
